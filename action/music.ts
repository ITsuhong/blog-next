import {MusicFormSchemaType} from "@/schema/music";
import prismadb from "@/lib/prisma";

export async function CreateMusic(value: MusicFormSchemaType) {
    try {
        // prismadb.music.create({
        //
        // })
    } catch {

    }
}